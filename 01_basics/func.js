function sum(a,b) {
    return a+b;
}

console.log(sum (3,4))

function vowel(x){
    let count=0;
    for(let i=0; i<x.length; i++){
        if(x[i]==="a" || x[i]==="e" ||x[i]==="i" ||x[i]==="o" ||x[i]==="u"){
            count++;
        
        }
    }console.log(count)
}

vowel("mansi")

